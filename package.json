{
    "name": "nyx-discord-bot",
    "version": "2.3.0",
    "description": "NYX External Discord license administration bot",
    "main": "bot.js",
    "engines": {
        "node": ">=22"
    },
    "scripts": {
        "start": "node bot.js",
        "check": "node --check bot.js && node --check index.js && node --check src/*.js && node --check src/handlers/*.js",
        "test": "npm run check && node --test \"test/*.test.js\"",
        "lint": "eslint .",
        "format": "prettier --write ."
    },
    "dependencies": {
        "discord.js": "^14.14.1"
    },
    "devDependencies": {
        "eslint": "^10.8.0",
        "prettier": "^3.9.6"
    }
}
